package org.example;

public class LargestPerimeterTriangle {
    public int largestPerimeter(int[] nums) {
        int perimeter = 0, longest = 0, middle = 0, shortest = 0, index = 0;
        int numberOfSides = nums.length;
        while (index < numberOfSides) {
            if (index == 0) {
                longest = nums[index];
            } else if (index == 1) {
            }
        }
        return perimeter;
    }
}